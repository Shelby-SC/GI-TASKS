
/* ************** EJERCICIO CONVERSIÓN DE TEMPERATURAS ************** */

/*

function conversor() {
    let celsius= document.getElementById("temperaturas").value;
    if (parseInt(celsius)) {
        let kelvin = parseInt(celsius) + 273;
        document.write("Esta es la temperatura en kelvin: " + kelvin + "<br>");
        let fahrenheit = (celsius*1.8) + 32;
        document.write("Esta es la temperatura es Fahrenheit: " + fahrenheit + "<br>");
    } else {
        document.write("Temperatura ingresada no es un número");
    }
}

botonConvertir.addEventListener("click", conversor);
 
*/

/*************** EJERCICIO FACTORIAL ******************/

/*
function factorial(){
    let value1 = document.getElementById("ingreseNum").value;
    if (parseInt(value1)) {
        let result = 1;
        for (let i= 1; i <= value1; i++) {
            result *= i;
        }
        document.getElementById("resultados").innerHTML= "El resultado del factorial del valor " + value1 + " es: " + result;

    } else {
        document.getElementById("resultados").innerHTML= "No es un número";
    }
}

*/

